part of '../status_bar.dart';

class StatusBarCenterContent extends StatelessWidget {
  const StatusBarCenterContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      children: [],
    );
  }
}
